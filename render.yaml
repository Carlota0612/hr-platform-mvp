services:
  - type: web
    name: peopleos-hr-platform
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: SESSION_SECRET
        generateValue: true
      - key: ADMIN_EMAIL
        value: carlota.moron.ortiz@h-partners.com
      - key: ADMIN_PASSWORD
        value: CarOrt2026!!
